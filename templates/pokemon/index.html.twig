{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}





{% block body %}
    <div class="row">
        {% for pokemons in pokemons %}
            <div class="col-sm-2">
                <div class="card" style="width: 18rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{ pokemons.name }} <img src="{{ pokemons.icon }}"></h5>
                        <img width="120px" src="{{ pokemons.image }}">
                        <p> {{ pokemons.life }}</p>
                        <p> {{ pokemons.life}}</p>
                    </div>
                </div>
            </div>
        {% endfor %}

    </div>
{% endblock %}
