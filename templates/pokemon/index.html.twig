{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}

    <div>
        {#{% for pokemon in pokemons %}
            <p>{{ pokemon.name }}</p>
            <img width="120px" src="{{ pokemon.image }}">
            <img src="{{ pokemon.icon }}">
        {% endfor %}#}

        {% for pokemon in pokemon %}
            <p>{{ pokemon.name }}</p>
            <img width="120px" src="{{ pokemon.icon }}">
        {% endfor %}
    </div>
{% endblock %}
